class TowersOfHanoi {
  run () {
    // until two posts are empty
    // and remaining post is not same
    // as original post
    // get first post from player
    // get second post from player
    // move disc if valid move
  }
}